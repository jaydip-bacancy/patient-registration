/**
 * System user ID for audit trails when actions are performed without an authenticated user
 * (e.g. public patient registration). This user must exist in the database - run seed to create it.
 */
export const SYSTEM_USER_ID = '00000000-0000-0000-0000-000000000001';
